# Backend Environment Variables for Production
# Copy these to your hosting platform (Render, Railway, etc.)

NODE_ENV=production
PORT=8000

# MongoDB Connection (use your MongoDB Atlas connection string)
MONGODB_URI=mongodb+srv://bookbazaar:bookbazaar123@cluster0.rs49bg4.mongodb.net/bookbazaar

# JWT Secret (CHANGE THIS to a secure random string for production!)
JWT_SECRET=your_super_secure_jwt_secret_key_change_this_in_production

# Frontend URL (add your Vercel URL after deployment)
CLIENT_URL=https://your-app.vercel.app

# Optional: Node version
NODE_VERSION=18.x
